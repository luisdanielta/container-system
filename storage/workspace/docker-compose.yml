services:
  workspace:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: workhand
    restart: unless-stopped
    command: tail -f /dev/null
    volumes:
      - data:/data
      - research:/research

volumes:
  research:
    external: false
  data:
    external: false